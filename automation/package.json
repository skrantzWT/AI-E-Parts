{
  "name": "ai-e-parts-automation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "codegen": "npx playwright codegen",
    "install": "npm install"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "playwright": "^1.49.0",
    "typescript": "^5.8.0"
  }
}
